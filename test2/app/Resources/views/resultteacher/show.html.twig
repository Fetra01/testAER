{% extends 'base.html.twig' %}

{% block body %}
    <h1>Resultteacher</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ resultTeacher.id }}</td>
            </tr>
            <tr>
                <th>Teacher</th>
                <td>{{ resultTeacher.teacher }}</td>
            </tr>
            <tr>
                <th>Survey</th>
                <td>{{ resultTeacher.survey }}</td>
            </tr>
            <tr>
                <th>Resultstudent</th>
                <td>{{ resultTeacher.resultStudent }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('resultteacher_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('resultteacher_edit', { 'id': resultTeacher.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
